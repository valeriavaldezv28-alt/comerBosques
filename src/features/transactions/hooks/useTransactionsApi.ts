/**
 * Hook futuro para conectar Transacciones con backend.
 *
 * Cuando la vista crezca, este hook deberia encargarse de:
 *
 * - pedir datos con TanStack Query;
 * - exponer loading/error/refetch;
 * - mantener cache por filtros;
 * - aislar a la vista de detalles de endpoints.
 */
export {};
