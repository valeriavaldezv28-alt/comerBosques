/**
 * Mapper futuro para Transacciones.
 *
 * Traducira la respuesta del backend al tipo `Transaction` usado por la UI.
 * Esto sera util si backend responde con nombres como `merchant_id`,
 * `created_at` o `transaction_status`.
 *
 * Por ahora el mock ya usa la forma esperada por `types/transaction.ts`.
 */
export {};
