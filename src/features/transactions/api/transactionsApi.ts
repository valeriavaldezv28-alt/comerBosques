/**
 * Capa API futura para Transacciones.
 *
 * Esta vista es solo para administradores. Cuando se conecte al backend, aqui
 * deberian vivir endpoints como:
 *
 * - GET /api/v1/transactions?page=1&limit=20
 * - GET /api/v1/transactions/:id
 * - GET /api/v1/transactions?merchantId=...
 *
 * Debe usar `apiClient` y respetar permisos backend, no solo el guard frontend.
 */
export {};
